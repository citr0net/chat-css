To Use:

file:///chat-overlay.html?session=[SessionID]&direction=right&pfp=false

* `&pfp` allows you to change pfp being enabled. Options are `True/False`
* `&direction` allows you to change the position of the chat. Options are `left/center/right`
* `?session` is the base session id for Social Stream Ninja
